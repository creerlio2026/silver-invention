"""AI service placeholder: functions to call model(s) and preprocess/postprocess."""


def analyze_text(text: str) -> dict:
    """Pretend to call an AI model and return a simple analysis."""
    return {"length": len(text), "summary": text[:100]}
